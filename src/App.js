import NavBar from "./components/NavBar";
import styled from "styled-components";

function App() {
    return (
        <StyledApp>
            <NavBar />
        </StyledApp>
    );
}

const StyledApp = styled.div``;

export default App;
